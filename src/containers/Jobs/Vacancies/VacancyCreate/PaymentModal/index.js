import styles from './styles.module.scss';

export default function PaymentModal(props) {
  const { open, onCardChange, type, ...rest } = props;

  return null;
}

PaymentModal.displayName = 'PaymentModal';

PaymentModal.propTypes = {};
